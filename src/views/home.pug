doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        title 노근본채팅
    body 
        header 
            h1 노근본채팅
            a(href="/video_call") 영통하러가기 
        main 
            #welcome 
                h4 개설된 방
                ul 
                form
                    input(type="text", placeholder="방 이름 입력", required)
                    button 방 들어가기
            #room 
                h3
                form#nickname
                    input(type="text", placeholder="닉네임을 정하세요", required)
                    button 저장
                ul 
                form#msg
                    input(type="text", placeholder="메세지를 입력하세요", required)
                    button 보내기
        script(src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js")